import React from 'react';
import { Form } from 'semantic-ui-react';

export const AzureOnlineServices = () => {
  return (
    <Form.Field>
      11. Navigate to <i>Cost Management + Billing</i>. Here we will be assigning our new application access to read
      billing and cost data. These steps change depending on the Billing Account type set for your Azure account
    </Form.Field>
  );
};

export default AzureOnlineServices;
